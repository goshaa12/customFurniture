---
// AssemblyCalculator.astro
import { User, FileText, Phone } from '@lucide/astro';
---

<section id="calculator" class="w-full py-16 md:py-24 bg-white">
  <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
    {/* Header */}
    <div class="text-center mb-12">
      <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold text-gray-900 mb-4">
        Калькулятор цены сборки
      </h2>
      <p class="text-base md:text-lg text-gray-600 leading-relaxed">
        Рассчитайте стоимость услуги сборки мебели заранее — это бесплатно и займёт меньше минуты. Введите данные ниже, чтобы получить предварительную цену и выбрать удобное время визита мастера.
      </p>
    </div>

    {/* Form Card */}
    <div class="bg-white rounded-2xl p-6 md:p-8 border border-gray-200 shadow-lg">
      <form class="space-y-6">

        {/* Имя */}
        <div class="space-y-2">
          <label class="flex items-center text-base font-semibold text-gray-900 mb-2">
            <div class="w-8 h-8 rounded-lg bg-purple-100 flex items-center justify-center text-purple-600 mr-3">
              <User size={18} />
            </div>
            Имя
          </label>
          <input 
            type="text" 
            placeholder="Введите ваше имя" 
            class="w-full border border-gray-300 rounded-lg px-4 py-3 text-gray-700 placeholder-gray-400 focus:outline-none focus:border-purple-400 focus:ring-2 focus:ring-purple-100 transition"
          />
        </div>

        {/* Описание задачи */}
        <div class="space-y-2">
          <label class="flex items-center text-base font-semibold text-gray-900 mb-2">
            <div class="w-8 h-8 rounded-lg bg-blue-300 flex items-center justify-center text-blue-600 mr-3">
              <FileText size={18} />
            </div>
            Кратко опишите задачу
          </label>
          <textarea 
            rows="4"
            placeholder="Например: сборка шкафа-купе 2м, кровати с матрасом и двух тумбочек" 
            class="w-full border border-gray-300 rounded-lg px-4 py-3 text-gray-700 placeholder-gray-400 focus:outline-none focus:border-blue-400 focus:ring-2 focus:ring-blue-100 transition resize-none"
          ></textarea>
        </div>

        {/* Телефон */}
        <div class="space-y-2">
          <label class="flex items-center text-base font-semibold text-gray-900 mb-2">
            <div class="w-8 h-8 rounded-lg bg-red-100 flex items-center justify-center text-red-600 mr-3">
              <Phone size={18} />
            </div>
            Телефон
          </label>
          <input 
            type="tel" 
            placeholder="+7 (___) ___-__-__" 
            class="w-full border border-gray-300 rounded-lg px-4 py-3 text-gray-700 placeholder-gray-400 focus:outline-none focus:border-red-400 focus:ring-2 focus:ring-red-100 transition"
          />
        </div>

        {/* Submit Button */}
        <div class="pt-4">
          <button 
            type="submit"
            class="w-full px-8 py-4 bg-amber-600 hover:bg-amber-700 text-white font-semibold rounded-lg transition-all duration-200 shadow-md hover:shadow-lg"
          >
            Рассчитать
          </button>
        </div>

      </form>
    </div>
  </div>
</section>